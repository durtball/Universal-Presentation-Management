# UPM Site

Site is the independently deployable local operational system. The same Site stack runs on a standalone Linux appliance or alongside Central without sharing Central processes, storage, configuration, or PostgreSQL.
