# Central Caddy Configuration

Development-only routing skeleton for the Central edge. Production TLS and hostname policy will be added deliberately without distributing certificate logic into application services.
