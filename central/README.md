# UPM Central

Central is the independently deployable global control plane. Its API, browser UI, durable workers, synchronization service, Caddy edge, and PostgreSQL database remain separate service boundaries.
